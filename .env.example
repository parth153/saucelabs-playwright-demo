# Copy to `.env` and adjust as needed. The suite falls back to these defaults
# when the variables are unset, so a .env file is optional.

# Base URL of the system under test.
BASE_URL=https://www.saucedemo.com

# Credentials used by auth.setup.ts to create the shared logged-in storage state.
STANDARD_USER=standard_user
PASSWORD=secret_sauce
